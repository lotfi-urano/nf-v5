name: Build APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        run: |
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
          mkdir -p $HOME/android-sdk/cmdline-tools
          unzip -q commandlinetools-linux-11076708_latest.zip -d $HOME/android-sdk/cmdline-tools
          mv $HOME/android-sdk/cmdline-tools/cmdline-tools $HOME/android-sdk/cmdline-tools/latest
          yes | $HOME/android-sdk/cmdline-tools/latest/bin/sdkmanager --licenses
          $HOME/android-sdk/cmdline-tools/latest/bin/sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - run: echo "sdk.dir=$HOME/android-sdk" > local.properties

      - run: echo "GEMINI_API_KEY=OFFLINE_MODE" > app/.env

      - name: Build Debug APK
        run: |
          export JAVA_HOME=$(echo $JAVA_HOME_17_X64)
          export PATH=$JAVA_HOME/bin:$PATH
          java -jar gradle/wrapper/gradle-wrapper.jar :app:assembleDebug --no-daemon
        env:
          ANDROID_SDK_ROOT: /home/runner/android-sdk

      - uses: actions/upload-artifact@v4
        with:
          name: nasheet-apk
          path: app/build/outputs/apk/debug/app-debug.apk

